package com.lyhorng.pedssystem.repository.branchRequest;

import org.springframework.data.jpa.repository.JpaRepository;

import com.lyhorng.pedssystem.model.branchRequest.RequestFor;

public interface RequestForRepository extends JpaRepository<RequestFor, Long> {
}
